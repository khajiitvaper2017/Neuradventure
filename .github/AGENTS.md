# .github Guidelines

- Keep automation config minimal and repo-focused (currently just `dependabot.yml`).
- Prefer safe defaults: group dependency updates where possible and avoid noisy schedules.
- Don’t add legacy compatibility workflows unless required; remove unused rules when editing this folder.

